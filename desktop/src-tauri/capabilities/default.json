{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability — the UI talks only to the localhost API.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
