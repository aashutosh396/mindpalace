# mindpalace core repo — NEVER commit user data or secrets.
# All instance data lives OUTSIDE this repo (default ~/.mindpalace).

__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
build/
dist/
.DS_Store

# never, ever
*.token
*secret*
*.pem
*.key
.claude_token
config.json

# skillforge raw harvest (staging) — installed skills live in mindpalace/skills/global/
_skillforge/staging/
_skillforge/staging*/
_skillforge/new_manifest.json
